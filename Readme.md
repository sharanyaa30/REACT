# REACT

- makes easy tp manage and build complex frontend.

- react is library as well as framework 

- uses other libraries to render certain 

- browser react DOM

- Mobile react native

-- {username} evaluated expression

-- data updates through hooks

- react fiber - split rendering work into chunks and spread it out over multiple frames, pause work and come back later, reuse the completed work , assign priority to different types of work

- hydration process is whn java script comes under use 

- reconcilation - react uses to differentiate one tree with another to determine which parts need to be changed

- keys are used to increase performance of list 

